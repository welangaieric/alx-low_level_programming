#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char myString[120] = "\"Programming is like building a multilingual puzzle";

	puts(myString);
	return (0);
}
