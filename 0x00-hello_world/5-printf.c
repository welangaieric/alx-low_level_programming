#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char myString[120]="with proper grammar, but the outcome is a piece of art,\n";

	printf("%s",myString);	
	return (0);
}
